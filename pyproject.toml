[project]
name = "e-ai"
version = "0.1.0"
description = "A small, maintainable CIFAR-10 training project."
requires-python = ">=3.10"
dependencies = [
    "matplotlib",
    "pretty-errors",
    "rich",
    "torch",
    "torchvision",
]

[tool.ruff]
line-length = 100
target-version = "py310"
